server:
  port: 8888

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: 12456
  jpa:
    show-sql: true
    open-in-view: false

logging:
  level:
    org.hibernate: trace
